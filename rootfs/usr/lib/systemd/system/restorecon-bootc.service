[Unit]
Description=Fix SELinux file system labels for bootc systems
After=local-fs.target
Wants=local-fs.target

[Service]
Type=oneshot
TimeoutSec=infinity
ExecStart=/bin/restorecon-bootc
ExecStart=/bin/systemctl --no-reload disable %n

[Install]
WantedBy=multi-user.target
